files:
  "/etc/nginx/conf.d/proxy_buffers.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      proxy_buffers 8 16k;
      proxy_buffer_size 32k;
      client_max_body_size 100M;
